
    <%_ if (hasServiceImpl) { _%>
    @Override
    <%_ } _%>
    <%_ if (databaseType === 'sql') { _%>
    @Transactional
    <%_ } _%>
    public <%= entityOrDtoClass %> persistOrUpdate(<%= entityOrDtoClass %> <%= entityOrDtoInstance %>) {
        log.debug("Request to save <%= entityClass %> : {}", <%= entityOrDtoInstance %>);
<%- include('../common/save', {...this, viaService: false, returnDirectly: true, isController: false}); -%>
    }
