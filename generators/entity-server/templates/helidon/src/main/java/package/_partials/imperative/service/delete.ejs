
    /**
     * Delete the <%= entityClass %> by id.
     *
     * @param id the id of the entity.
     */
    <%_ if (hasServiceImpl) { _%>
    @Override
    <%_ } _%>
    <%_ if (databaseType === 'sql') { _%>
    @Transactional
    <%_ } _%>
    public void delete(<%= primaryKeyType %> id) {
        log.debug("Request to delete <%= entityClass %> : {}", id);
<%- include('../common/delete', {...this, viaService: false}); -%>
    }
